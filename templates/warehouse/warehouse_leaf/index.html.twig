<div id="warehouse-leaf-section">
    <div id="go-back-button-container" class="mb-3">
        <button id="warehouse-structure-go-back" class="btn btn-success"
                data-endpoint="{{ path('app_warehouse_structure_open', {id: node.parent.id}) }}"
                data-nodeId="{{ node.parent.id }}">
            <i class="fa-solid fa-arrow-left me-2"></i>
            Powrót
        </button>
    </div>
    <section id="warehouse-leaf-configuration-form">
        {{ render(controller('App\\Warehouse\\Controller\\WarehouseLeafController::saveConfiguration', {'id': node.id})) }}
    </section>
    <section id="warehouse-leaf-items-table" class="mt-4">
        {{ render(controller('App\\Warehouse\\Controller\\WarehouseLeafController::renderItemsTable', {'id': node.id})) }}
    </section>
    <input type="hidden" id="render-items-table-endpoint" value="{{ path('app_warehouse_leaf_items_table', {'id': node.id}) }}">
</div>