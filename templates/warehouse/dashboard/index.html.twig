{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid mt-3 mb-5">
        <div class="row">
            <div class="col-xs-12 col-md-6">
                {{ render_chart(countChart) }}
            </div>
            <div class="col-xs-12 col-md-6">
                {{ render_chart(changesChart) }}
            </div>
        </div>
    </div>
{% endblock %}